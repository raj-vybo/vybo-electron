import emailValidator from './email.validator';
import nameValidator from './name.validator';
import { passwordValidator, confirmPassValidator } from './password.validator';
import codeValidator from './verification-code.validator';

export {
  nameValidator,
  emailValidator,
  passwordValidator,
  confirmPassValidator,
  codeValidator,
};
